#include <bits/stdc++.h>
using namespace std;

int main() {                    

    // Intro to C++.
    // Using cin, cout not scanf, printf.
    // To print new line : '\n' or endl;
    // cout << a << '\n';
    // cout << a << endl;


    // floor(n), ceil(n), log2(n), log10(n), sqrt(n), sqrtl(n), pow(base, power), __gcd(a, b). 
    // lcm = (a * b) / __gcd(a, b).
    // pi = acos(-1).
    // set precision : 
    //                cout << fixed << setprecision(10) << n << endl;


    // homework
    // 1. Find log7(100).
    // 2. Find x from ax^2 + bx + c = 0.
    // 3. Find degree value from radians.

    return 0;
}
